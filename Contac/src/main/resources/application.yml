spring:
  application:
    name: contac

  datasource:
    url: jdbc:mysql://localhost:3306/contac_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 30MB
      max-request-size: 30MB

  security:
    user:
      name: admin
      password: admin123

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

jwt:
  secret: Y29udGFjX3NlY3JldF9rZXlfMjAyNF9zdXBlcl9zZWd1cmFfcGFyYV90b2tlbnNfand0X2Jhc2U2NA==
  expiration: 86400000

app:
  admin:
    default:
      username: admin
      password: Admin123!